# This file is not a full hyprland configuration.
# It is intended to be included in your main hyprland.conf.

$activeBorderColor = rgb(2895A3)
$foreground        = rgb(BFBDB6)  # Soft white — primary text
$foreground_trans  = rgba(BFBDB655)  # Soft white — primary text
$background        = rgb(0D1017)  # Deep dark — editor background
$green             = rgb(7FD962)  # Bright green — strings, additions
$accent1           = rgb(E6B450)  # Orange/gold — keywords, highlights
$accent1_trans     = rgba(E6B45055)  # Orange/gold — keywords, highlights
$accent2           = rgb(39BAE6)  # Cyan/blue — functions, types
$accent2_trans     = rgba(39BAE655)  # Cyan/blue — functions, types
$green_trans       = rgba(7FD96255)  # Bright green — strings, additions
# general {
#     col.active_border = $activeBorderColor
# }
#
# group {
#     col.border_active = $activeBorderColor
# }
